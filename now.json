{
  "version": 2,
  "builds": [
    {
      "src": "*.js",
      "use": "@now/node-server"
    }
  ],
  "env": {
    "MONGO_URI": "@mongo-uri"
  },
  "routes": [{ "src": "/api/.*", "dest": "/index.js" }]
}
